// third-party libraries
import request from "supertest";

// API
import userController from "../userControllers";

// test suite
describe("Users Module", () => {
  it("GET should fetch all users", async () => {
    request();
  });
});
